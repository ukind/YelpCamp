<%include ../partials/header %>
  <div class="container my-5">
    <div class="row">

    </div>
    <div class="row camperForm">

      </div>
    </div>
  </div>

  <script type="text/javascript">
    const newCommentForm = document.querySelector('.camperForm');
    const camperComment = <%-JSON.stringify(camperComment) %>
    console.log(camperComment);
    newCommentForm.innerHTML =
      `
      <div class="col-md-12">
        <h1 class="text-center">Add new comment for ${camperComment.name.first}</h1>
      </div>
      <div class="col-md-7 mx-auto">
        <form class="" action="/camper/${camperComment._id}/comment" method="post">
          <div class="form-group w-50">
            <label>Author</label>
          <input class="form-control" placeholder="Author" name="comment[author]" type="text">
        </div>
        <div class="form-group">
          <label for="comment">Comment</label>
          <textarea class="form-control" id="exampleTextarea" rows="3" placeholder="Add comment..." name="comment[text]"></textarea>
        </div>
          <button type="submit" class="btn btn-primary">Add</button>
        </form>
  `;
  </script>

  <%include ../partials/footer %>
